"""Package version — single source of truth.

Importing from a dedicated module rather than the package ``__init__``
keeps the package init free of code, per project convention.
"""

__version__ = "0.1.0"
