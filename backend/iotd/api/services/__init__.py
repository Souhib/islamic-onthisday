"""Outward integrations live here (mail, queues, third-party APIs).

Empty for v0 — the API is read-only against the local SQLite. Add modules
as integrations land (e.g. ``email.py``, ``analytics.py``).
"""
