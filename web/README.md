# Islamic On-This-Day — Web client

Vite + React + TanStack Router reading surface for the Islamic On-This-Day
project. Trilingual (en / fr / ar with RTL), light/dark theme via CSS
variables, custom editorial design system, TanStack Query backed by an
auto-generated [`@hey-api/openapi-ts`](https://github.com/hey-api/openapi-ts)
client.

## Stack

| Concern | Choice |
|---|---|
| Package manager | **Bun** |
| Bundler | **Vite 6** (esbuild minify, vendor `manualChunks`) |
| Language | **TypeScript 5** (strict) |
| Framework | **React 18** |
| Routing | **TanStack Router** (file-based, code-split) |
| Server state | **TanStack Query** |
| HTTP | **`@hey-api/client-fetch`** (the only client — no `ky`, no `axios`) |
| API codegen | **`@hey-api/openapi-ts`** (regenerated from `web/openapi.json`) |
| Styling | **Tailwind CSS v4** with **CSS variables** (`@theme inline`) |
| Theme | `.dark` class on `<html>` (toggled by `ThemeProvider`) |
| i18n | **i18next** + lazy-loaded JSON locales, `react-i18next` hooks |
| A11y primitives | **`@radix-ui/react-dialog`** for the disputed-views drawer |
| Errors | **`@sentry/react`** (gated on `VITE_SENTRY_DSN`) |
| Analytics | **Umami** (self-hosted, gated on `VITE_UMAMI_URL` + `VITE_UMAMI_WEBSITE_ID`) |
| Linting | **oxlint** |
| Formatting | **oxfmt** |
| Testing | **Vitest** + Testing Library + happy-dom |
| Path alias | `@/*` → `./src/*` |
| Dev proxy | `/api` → `http://127.0.0.1:5111` |

## Commands

```sh
bun install
bun run dev                  # vite on :3000, proxies /api → :5111
bun run build                # production bundle
bun run preview              # serve the production build locally
bun run typecheck            # tsc -b --noEmit
bun run lint                 # oxlint
bun run lint:fix             # oxlint --fix
bun run format               # oxfmt
bun run format:check         # oxfmt --check (CI gate)
bun run test                 # vitest (run once)
bun run test:watch           # vitest --watch
bun run check                # typecheck + lint + test (the full gate)
bun run generate-api         # regen the hey-api client from openapi.json
```

## Project layout

```
src/
├── main.tsx                       # router boot + Sentry init + i18n boot
├── index.css                      # Tailwind import + design tokens (CSS vars) + helper classes
├── vite-env.d.ts                  # ImportMetaEnv types for VITE_* vars
├── routeTree.gen.ts               # generated by TanStack Router (committed)
├── i18n/
│   ├── index.ts                   # i18next bootstrap (lazy loader)
│   ├── months.ts                  # Hijri month name constants (single source of truth)
│   └── locales/{en,fr,ar}.json    # UI strings — content i18n is on the API
├── api/
│   ├── client-setup.ts            # hey-api client baseUrl
│   ├── queryKeys.ts               # central queryKey map (invalidation / prefetch)
│   ├── generated/                 # @hey-api/openapi-ts output — DO NOT EDIT
│   └── {today,events,lessons,observances,people,list}.ts  # thin TanStack Query hooks
├── components/
│   ├── design/                    # editorial primitives (FriezeRule, EightPointStar, …)
│   │   ├── primitives.tsx         #   Tailwind-based, dark-aware via .dark class
│   │   └── primitives.test.tsx
│   ├── disputed/                  # DisputeBadge + DisputedDrawer (Radix dialog)
│   ├── reader/                    # Masthead, DetailHeader, Footer, LeftRail, RightRail,
│   │                              # EventCard, LessonReader, Main, PageShell, …
│   └── ui/                        # Loading, Empty, NotFound, ErrorBoundary
├── lib/
│   └── utils.ts                   # cn() — clsx + tailwind-merge
├── providers/
│   ├── ThemeProvider.tsx          # toggles html.dark + persists to localStorage
│   ├── LanguageProvider.tsx       # binds i18next.language, exposes pickLocalised()
│   └── QueryProvider.tsx          # TanStack Query client
├── routes/                        # TanStack file-based routing
└── test/
    └── setup.ts                   # jest-dom matchers
```

## Design system

The project ships **custom Tailwind components, not shadcn/ui**. The
editorial visual language (frieze rosettes, eight-point stars, dot-chip
verification chips, mono-uppercase eyebrows, paper-cream palette) is
bespoke enough that shadcn's defaults would have been re-themed top to
bottom. Radix primitives are pulled in *selectively* for a11y-critical
interactive components only — currently `@radix-ui/react-dialog` under
`DisputedDrawer`.

**Tokens live in `src/index.css`** as CSS custom properties on `:root`
and `.dark`. The `@theme inline` block re-exports them as Tailwind colour
/ font scales so utilities like `text-ink`, `bg-paper`, `font-mono` read
the same vars. Dark mode flips via `<html class="dark">` — no component
branches on it. To add a new colour token, add it to both `:root` and
`.dark` and to the `@theme inline` map.

### Design language — what to preserve when extending

- **Type stack** — Cormorant Garamond (serif headlines + body), Inter
  Tight (UI/sans), JetBrains Mono (eyebrows, captions, metadata), Amiri
  (Arabic).
- **Colour** — warm cream paper (`#F5F0E6`) / deep ink-blue dark
  (`#0F1217`). Single accent: a muted oxidised-copper / verdigris
  (`oklch(58% 0.06 165)`) used only for small architectural marks and
  verification chips. No gradients, no glow.
- **Frieze language** — the architectural section divider is the
  system's cohesion device. Heavy top rule + hairline below + small
  geometric rosette centred. Use `FriezeRule` for any new section break.
- **Image policy** — when an event has no image (`noImage: true` in
  data), render `NoImagePlate`, never an empty slot.
- **Disputes are visible** — when `event.disputed` is true, surface the
  `DisputedDrawer`. Every classical position is weighted explicitly.

## API client lifecycle

The OpenAPI client is generated from `web/openapi.json`. **Never edit
`src/api/generated/`.** Regenerate after backend schema changes:

```sh
# from backend/
uv run python -c "import json; from iotd.app import create_app; print(json.dumps(create_app().openapi()))" \
  > openapi.json && cp openapi.json ../web/openapi.json
# from web/
bun run generate-api
```

Hand-rolled hooks in `src/api/{today,events,lessons,…}.ts` are thin
wrappers around the generated `*Options` helpers — they exist to expose
`enabled` toggles and to centralise per-route defaults. The query-key
map in `src/api/queryKeys.ts` is the source of truth for invalidation:
reach for it instead of re-deriving the option object at the callsite.

## Conventions

1. **Custom Tailwind, not shadcn.** Visual fidelity comes first — the
   project's editorial design isn't shadcn-shaped.
2. **CSS variables, not JS branching.** Theme switching happens once at
   the root; nothing reads `theme === "dark"` except `ThemeProvider`.
3. **i18next for UI strings.** `useTranslation()`'s `t(...)` for chrome,
   `pickLocalised(...)` for content from the API payload (different
   system, different concern).
4. **No `ky`, no second HTTP client.** When auth lands, configure the
   generated client via `client.setConfig(...)` or interceptors.
5. **Routes have zero per-page chrome duplication.** Use `DetailHeader`
   for *-detail pages; use `PageShell` for landing pages with a subtitle
   and Footer.
6. **Loading / Empty / NotFound / ErrorBoundary live in `components/ui/`.**
   Don't reinvent per-route variants.
7. **Hijri month names** come from `@/i18n/months.ts` — never redeclare
   the array in a route file.

## Analytics

Umami is wired through `src/lib/analytics.ts`. Init is gated on
`VITE_UMAMI_URL` + `VITE_UMAMI_WEBSITE_ID` — without those env vars the
`<script>` is never injected and every helper is a silent no-op.

The tracked events are deliberately curated (no theme toggles, no
filter clicks, no pagination — just signals that answer real product
questions):

- **page view** — auto on every route change (in `__root.tsx`).
- **`event_view` / `lesson_view` / `observance_view` / `person_view`** —
  fired in the detail-page `useEffect`, keyed on the slug. Tracks what
  people read.
- **`search`** — `term` (trimmed + capped at 80 chars) + `results`;
  fired only on a successful query resolution against the deferred
  value, so fast typing doesn't generate one event per keystroke.
- **`language_change`** — `language` (en/fr/ar); fired from
  `LanguageSwitcher` when the user actively changes the language.
- **`dispute_opened`** — `slug` of the event whose disputed-views
  drawer was opened. Strong signal of editorial engagement.

Add new helpers in `src/lib/analytics.ts` (not at the call site) so the
event taxonomy stays auditable in one place.

## Environment variables

| Var                       | Purpose                                                |
| ------------------------- | ------------------------------------------------------ |
| `VITE_API_URL`            | Backend origin for the generated client. Default: `window.location.origin`. |
| `VITE_SENTRY_DSN`         | Sentry DSN; empty disables the SDK (default in dev).   |
| `VITE_UMAMI_URL`          | Umami host (e.g. `https://analytics.iotd.app`); empty disables. |
| `VITE_UMAMI_WEBSITE_ID`   | Umami site UUID. Empty disables.                       |
| `VITE_APP_VERSION`        | Auto-injected from `package.json` at build time. Don't set manually. |

See `.env.example` for the full list.
